<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_pq_start"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">
    <!--
    tools:context="com.example.cs465.personality_quiz">
    -->

    <TextView
        style="@style/TitleBarText"
        android:text="@string/pq_titlebar" />

    <Button
        android:text="@string/pq_startButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:id="@+id/pq_startButton"
        android:onClick="startQuiz"/>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@android:drawable/presence_busy"
        android:layout_above="@+id/pq_startButton"
        android:layout_alignEnd="@id/pq_startButton"
        android:id="@+id/imageButton"
        android:contentDescription="@string/pq_start_text"/>


</RelativeLayout>